class ArticleModel{

  String author;
  String title;
  String sourceName;
  String sourceId;
  String description;
  String url;
  String urlToImage;
  String publishedAt;
  String content;

  ArticleModel(
      {this.sourceName, this.sourceId, this.author, this.title, this.description, this.url, this.urlToImage, this.publishedAt, this.content});

}

